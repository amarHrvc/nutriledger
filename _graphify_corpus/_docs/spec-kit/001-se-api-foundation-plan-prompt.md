Feature: 001-se-api-foundation
Spec: specs/001-se-api-foundation/spec.md

Technical Context:
- BE: PHP 8.4, Laravel 12 (API-only), Sanctum, Pest 4, Larastan level 5, Pint
- FE: Vite, React 19, JavaScript (no TypeScript), Axios
- Storage: MySQL (prod), SQLite in-memory (tests)
- Testing: Pest 4 (BE); CORS verified via browser — no FE test suite this feature
- Project type: BE/FE — web API + browser SPA scaffold

Already implemented — do not re-plan:
- Models, migrations, factories: User, Patient, PatientSocioeconomic, Visit
- Policies: PatientPolicy, VisitPolicy
- RoleMiddleware: app/Http/Middleware/RoleMiddleware.php (registered in bootstrap/app.php)
- Fortify (web-only session auth) — must remain untouched

Reference docs — read during research phase:
- _docs/_shift/LARAVEL_API_RULES.md — API design rules, response format, URL conventions
- _docs/_shift/DB_SCHEMA_FINAL.md — authoritative schema
- _docs/_shift/IMPLEMENTATION_STATUS.md — what exists vs what's new

Constitution Check gates (.specify/memory/constitution.md v2.0.1):
- II: auth:sanctum middleware → FormRequest authorize() → Policy — all three
- III: Pest tests — happy path + validation failure + role/auth rejection
- IV: Pint → Larastan level 5 → affected tests — all before task done
- V: BE tasks and FE tasks always separate items

Source structure:
BE (Laravel root):
  routes/api.php                         ← new
  app/Http/Controllers/Api/              ← new
  app/Http/Traits/ApiResponses.php       ← new
  config/sanctum.php                     ← generated by install:api

FE (/frontend at repo root — new folder):
  src/api/client.js                      ← axios instance + baseURL + interceptor stub
  src/App.jsx                            ← smoke-test component calling GET /api/ping
  .env / .env.example
